From 87fcf9965d3c6ddf49c19082dc1f0b8430980692 Mon Sep 17 00:00:00 2001
From: Cowork <martinrf79@gmail.com>
Date: Thu, 10 Sep 2026 22:57:26 +0000
Subject: [PATCH] Orden, 2 de 2: las referencias VIVAS apuntan a papeles/

Ocho archivos de codigo abrian esos documentos por ruta y se romperian con el
movimiento: scripts/areas.py, scripts/inventario_fuente.py,
scripts/setup_test_env.sh y cinco bancos. Mas CLAUDE.md, el README y el de
tests. Todas actualizadas y comprobadas: no queda una mencion viva sin el
prefijo nuevo, y la bateria sigue en 500 pasan, 1 xfail.

LAS FICHAS Y LO APAGADO NO SE TOCAN, y es a proposito: una ficha del 26-ago que
dice DECISIONES.md esta contando donde vivia ese documento ESE DIA, y eso es
cierto. Reescribir el relato para que apunte a la ruta de hoy seria falsearlo.
MAPA.md lo deja dicho en una linea.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01FBwSUbK599gKC6Lh4WVJ4x
---
 CLAUDE.md                           | 8 ++++----
 README.md                           | 2 +-
 banco_pruebas/inventario_barrido.py | 4 ++--
 banco_pruebas/las_40.py             | 2 +-
 banco_pruebas/objetivo.py           | 4 ++--
 banco_pruebas/peso_del_censo.py     | 2 +-
 banco_pruebas/preguntas.py          | 2 +-
 scripts/areas.py                    | 2 +-
 scripts/inventario_fuente.py        | 2 +-
 scripts/setup_test_env.sh           | 6 +++---
 tests/README.md                     | 4 ++--
 11 files changed, 19 insertions(+), 19 deletions(-)

diff --git a/CLAUDE.md b/CLAUDE.md
index 117e672..4c003f3 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -43,5 +43,5 @@ entera; lo que se comprimió es el cuento. Si algo hace falta, `git log` lo tien
 
-**Qué NO va acá:** el estado (`RESUMEN_PARA_NUEVO_CHAT.md`), lo que falta
-(`PENDIENTE.md`), lo decidido (`DECISIONES.md`), cómo se ordena el sistema
-(`ARQUITECTURA.md`), y la unidad de trabajo abierta (`arquitectura/`).
+**Qué NO va acá:** el estado (`papeles/RESUMEN_PARA_NUEVO_CHAT.md`), lo que falta
+(`PENDIENTE.md`), lo decidido (`papeles/DECISIONES.md`), cómo se ordena el sistema
+(`papeles/ARQUITECTURA.md`), y la unidad de trabajo abierta (`arquitectura/`).
 
@@ -262,3 +262,3 @@ sube a Firestore por `/admin/upload-catalog` y `/admin/upload-faq`.
 **NO se escriben acá los números de la fuente** —cuántos productos, cuántos temas,
-cuántas categorías—. Están en `INVENTARIO_FUENTE.md`, que tiene candado que lo
+cuántas categorías—. Están en `papeles/INVENTARIO_FUENTE.md`, que tiene candado que lo
 obliga a coincidir con los archivos reales.
diff --git a/README.md b/README.md
index bfb53cc..4721b93 100644
--- a/README.md
+++ b/README.md
@@ -71,3 +71,3 @@ ENVIRONMENT=production
 
-**No se deploya a mano desde acá.** Hay un solo camino y está en `DEPLOY.md`: el
+**No se deploya a mano desde acá.** Hay un solo camino y está en `papeles/DEPLOY.md`: el
 push a `main` dispara `.github/workflows/deploy.yml`, que corre la batería de
diff --git a/banco_pruebas/inventario_barrido.py b/banco_pruebas/inventario_barrido.py
index 96e46cc..44a8c5d 100644
--- a/banco_pruebas/inventario_barrido.py
+++ b/banco_pruebas/inventario_barrido.py
@@ -31,3 +31,3 @@ arregla con este archivo: **un solo lugar, medido, con candado.**
 QUE HACE. Corre cada barrido, le pide su numero, y escribe
-`INVENTARIO_BARRIDO.md`. El candado `tests/test_inventario_barrido.py` vuelve a
+`papeles/INVENTARIO_BARRIDO.md`. El candado `tests/test_inventario_barrido.py` vuelve a
 medir y compara: si el documento y la medicion no coinciden, se pone rojo. Y
@@ -47,3 +47,3 @@ if str(_RAIZ) not in sys.path:
 
-DESTINO = _RAIZ / "INVENTARIO_BARRIDO.md"
+DESTINO = _RAIZ / "papeles/INVENTARIO_BARRIDO.md"
 
diff --git a/banco_pruebas/las_40.py b/banco_pruebas/las_40.py
index eca769b..ca4e482 100644
--- a/banco_pruebas/las_40.py
+++ b/banco_pruebas/las_40.py
@@ -15,3 +15,3 @@ precision sobre lo que SI esta en el repo, y cada entrada declara su fuente:
   - `RESUMEN etapa 1 #n`  : las 15 preguntas dificiles del 4-ago, verbatim.
-  - `CONSIGNA dialogo n`  : los 8 dialogos de CONSIGNA_PREGUNTAS_REALES.md.
+  - `CONSIGNA dialogo n`  : los 8 dialogos de papeles/CONSIGNA_PREGUNTAS_REALES.md.
   - `guion NN`            : los guiones que Martin mando por WhatsApp y quedaron
diff --git a/banco_pruebas/objetivo.py b/banco_pruebas/objetivo.py
index 284f689..7e26ebc 100644
--- a/banco_pruebas/objetivo.py
+++ b/banco_pruebas/objetivo.py
@@ -37,3 +37,3 @@ que es como lo escribio el. **Una sola redaccion no prueba nada.**
 EL HISTORIAL SOBREVIVE LAS SESIONES. Cada corrida con `--anotar` agrega una
-fila a `OBJETIVO.md`: fecha, que se cambio, la nota antes y despues. El archivo
+fila a `papeles/OBJETIVO.md`: fecha, que se cambio, la nota antes y despues. El archivo
 es GENERADO, nunca escrito a mano: un documento a mano miente a las dos
@@ -56,3 +56,3 @@ if str(_RAIZ) not in sys.path:
 
-DOC = _RAIZ / "OBJETIVO.md"
+DOC = _RAIZ / "papeles/OBJETIVO.md"
 TIENDA = "verifika_prod"
diff --git a/banco_pruebas/peso_del_censo.py b/banco_pruebas/peso_del_censo.py
index 81bae0d..c2ed5ba 100644
--- a/banco_pruebas/peso_del_censo.py
+++ b/banco_pruebas/peso_del_censo.py
@@ -7,3 +7,3 @@ casete) y cuenta, nodo por nodo: cuantas veces CORRIO y cuantas veces INTERVINO
 
-POR QUE EXISTE. `PASO0_CENSO.md` afirma que hay nodos que no intervienen NUNCA
+POR QUE EXISTE. `papeles/PASO0_CENSO.md` afirma que hay nodos que no intervienen NUNCA
 sobre el corpus. Sin este script eso es una afirmacion escrita a mano, y una
diff --git a/banco_pruebas/preguntas.py b/banco_pruebas/preguntas.py
index e9384d3..f288d2e 100644
--- a/banco_pruebas/preguntas.py
+++ b/banco_pruebas/preguntas.py
@@ -8,3 +8,3 @@ DE DONDE SALEN LAS CLASES, y esto importa: **no las invente**. Salen de lo que
 el repo ya tiene probado y sufrido -los 80 guiones, los 8 dialogos de
-`CONSIGNA_PREGUNTAS_REALES.md` y las 40 pruebas de `las_40.py`-. Cada clase
+`papeles/CONSIGNA_PREGUNTAS_REALES.md` y las 40 pruebas de `las_40.py`-. Cada clase
 declara de donde viene. Inventar clases nuevas seria medir mi imaginacion en vez
diff --git a/scripts/areas.py b/scripts/areas.py
index e410a2c..553713f 100644
--- a/scripts/areas.py
+++ b/scripts/areas.py
@@ -48,3 +48,3 @@ def principal() -> int:
     print("  fuente       catalogo, FAQ y tarifas: data/clientes/<tienda>/")
-    print("              mide: INVENTARIO_FUENTE.md (con candado)")
+    print("              mide: papeles/INVENTARIO_FUENTE.md (con candado)")
     print("=======================================================================")
diff --git a/scripts/inventario_fuente.py b/scripts/inventario_fuente.py
index 29da9e1..798aaf6 100644
--- a/scripts/inventario_fuente.py
+++ b/scripts/inventario_fuente.py
@@ -12,3 +12,3 @@ Uso:
     python scripts/inventario_fuente.py --vivo
-    python scripts/inventario_fuente.py --md INVENTARIO_FUENTE.md
+    python scripts/inventario_fuente.py --md papeles/INVENTARIO_FUENTE.md
 
diff --git a/scripts/setup_test_env.sh b/scripts/setup_test_env.sh
index e91f097..ba9e01c 100755
--- a/scripts/setup_test_env.sh
+++ b/scripts/setup_test_env.sh
@@ -77,3 +77,3 @@ if git rev-parse --git-dir >/dev/null 2>&1; then
       fi
-      echo "       NO TRABAJES ASI. Es la REGLA CERO BIS de ARRANQUE.md."
+      echo "       NO TRABAJES ASI. Es la REGLA CERO BIS de papeles/ARRANQUE.md."
       echo "       Miralo con: git status && git log --oneline origin/main..HEAD"
@@ -120,3 +120,3 @@ que son los que hacen algo en vez de pedirlo:
   - Cuantos productos, temas o barridos hay NO se escribe en ningun texto:
-    INVENTARIO_FUENTE.md e INVENTARIO_BARRIDO.md, que tienen candado.
+    papeles/INVENTARIO_FUENTE.md e papeles/INVENTARIO_BARRIDO.md, que tienen candado.
 
@@ -131,3 +131,3 @@ que son los que hacen algo en vez de pedirlo:
 
-Detalle historico: RESUMEN_PARA_NUEVO_CHAT.md. Reglas: CLAUDE.md.
+Detalle historico: papeles/RESUMEN_PARA_NUEVO_CHAT.md. Reglas: CLAUDE.md.
 =============================================================================
diff --git a/tests/README.md b/tests/README.md
index 615902a..2b66e70 100644
--- a/tests/README.md
+++ b/tests/README.md
@@ -135,3 +135,3 @@ un número o un nombre que el código también dice, gana el código y el `.md`
 mal. Ya pasó tres veces: la FAQ que decía 44 temas y tenía 50, el modelo del LLM
-mal nombrado durante meses en `CLAUDE.md`, y el `paths-ignore` de `DEPLOY.md`.
+mal nombrado durante meses en `CLAUDE.md`, y el `paths-ignore` de `papeles/DEPLOY.md`.
 Cada caso tiene ahora su candado.
@@ -140,3 +140,3 @@ Cada caso tiene ahora su candado.
 
-Leer `CLAUDE.md` (reglas) y `RESUMEN_PARA_NUEVO_CHAT.md` (estado), y correr
+Leer `CLAUDE.md` (reglas) y `papeles/RESUMEN_PARA_NUEVO_CHAT.md` (estado), y correr
 `pytest` para ver qué está en rojo. Con eso el chat arranca sabiendo todo y sigue
-- 
2.43.0


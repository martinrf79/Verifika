# PENDIENTE — lo que quedó abierto, con su estado

Este archivo es CORTO a propósito y lo imprime entero el hook de arranque, así
que toda sesión nueva lo lee sin buscarlo. Reglas: máximo veinte líneas de
contenido, un ítem por línea, y cada uno arranca con su estado.

Lo que se HIZO no va acá: eso lo cuenta `git log`, que el hook también imprime
y que nadie puede desactualizar. Acá va solo lo que falta.

Estados: **ABIERTO** (no empezado) · **A MEDIAS** (dice qué falta para cerrarlo)
· **ESPERA A MARTIN** (hecho lo que se podía, falta una decisión o un dato suyo).

Candado: `tests/test_pendiente_al_dia.py` falla si hay commits que tocan `app/`
más nuevos que la última edición de este archivo. No se cierra una sesión
dejándolo viejo.

---

- **ABIERTO** · **El rubro que cambia de producto quedó CERRADO en sus dos mitades, y el techo de cosas a medias está en CERO.** Entre turnos: el del carrito gana si sigue satisfaciendo lo que el cliente nombró. Dentro del turno: "2 mouse" ya no puede salir con un Genius y un Logitech — se unifica, porque el cliente nombró un rubro una vez y la variedad la inventaba el sistema. **Tres ataduras, cada una con test:** si el cliente declara dos items del mismo rubro no se toca; se unifica dentro de cada destino para no romper el reparto de envíos; y se agrupa por CATEGORÍA y no por `_cubierto`, que es laxo — **el barrido cazó que agrupando con el laxo se unificaban dos rubros distintos, o sea que el arreglo se comía mercadería pedida**. Lo que queda abierto es mirar si el mismo defecto puede entrar por otra puerta que no sea la reposición.
- **ABIERTO** · **Zona ciega: CERO, y las tres áreas que faltaban tienen barrido.** Todo lo que se puede probar offline tiene prueba; las 31 que quedan están declaradas con motivo en `banco_pruebas/sin_camino_offline.py` —el modelo, Firestore, el almacenamiento del lead y los conectores— con tres candados que no dejan usar la lista de atajo. Cuántos barridos hay NO se escribe acá: sale del generador y lo imprime `INVENTARIO_BARRIDO.md`. Ojo con lo que esto NO quiere decir: cero ciegas es que alguien las ejecuta, no que estén bien medidas, y un barrido mide su área, no la redacción del modelo. Lo que sigue sin barrido propio está en la sección de límites del inventario.
- **ABIERTO** · **Lo muerto se borró: SIN ALCANCE de 36 a 15, y las 15 que quedan NO son muertas.** Se fueron 16 funciones —6 de `indice.py`, 4 de `fuente_producto`, 3 de `guia_compra`, y una de compatibilidad, calculadora y grafo— más `posventa.py` entero, que se movió a `reserva/`. **Ojo con el instrumento, que casi cuesta una respuesta:** SIN ALCANCE dice DÓNDE MIRAR, no qué borrar. `_bloque_sin_dato` estaba en la lista y la llama `buscar_productos`, porque a las herramientas las despacha el MODELO por nombre y el grafo estático no ve esa arista; lo mismo las anidadas y los `__init__` de los conectores. Las 15 que quedan están todas vivas por app/, scripts/, tests/ o los bancos. Lo que sigue abierto es el otro eje: las funciones que sí se alcanzan pero nadie llama, que viven en `DECLARADAS` de `tests/test_nada_suelto.py` y hoy son 28.
- **ABIERTO** · **ALIVIAR LA CARGA DEL MODELO, y ya está medido dónde.** `banco_pruebas/peso_del_turno.py` mide qué viaja: **27.518 bytes, y el 92% es el esquema de las 9 herramientas** —las instrucciones son el 8%, así que limar prompts es limar la nada—. **Ojo con dónde pesa, que el encabezado del banco dice "cada llamada" y es impreciso: el esquema viaja SOLO en la llamada del decisor. `_redactar` no manda herramientas.** O sea que lo que se paga de más son las RONDAS: cada ronda extra vuelve a pagar los 25.370 bytes enteros. `consultar_temas` sola pesa 8.042, el 32%, casi todo su enum de 129 temas. Lo primero a atacar, en orden: **(1)** el enum de temas se acota por turno con una salida de escape libre que el código resuelve contra las 738 señas, y se loguea cada vez que se usa —así, si el prefiltro se come un tema, se ve en el log y no lo paga el cliente—; **(2)** no reenviar `registrar_pedido` en las rondas dos y tres, que son 3.046 bytes por ronda y no cambia comportamiento; **(3)** `tomar_pedido`, que no aparece en ninguna de las 15 charlas grabadas, se mide antes de sacarla. **Los enum de `categoria` y `campo` NO se tocan:** no están por peso, son la atadura que impide nombrar una categoría que no vendemos o un campo que la ficha no tiene. El prefiltro es superficie NUEVA y nace con su propio barrido sobre las mismas 738 señas. Todo esto TOCA `app/` y por lo tanto DEPLOYA.
- **ABIERTO** · **EL RECORTE: POR DONDE EMPIEZA, sin deducir nada.** El hook imprime `scripts/areas.py`: la etapa **salida tiene 18 nodos** y es la más cargada del turno; las otras cinco suman 14 juntas. **Ahí está la sobreingeniería y ahí se corta.** El dato para hacerlo sin fe ya está medido: `banco_pruebas/ablacion.py` dice que con TRES herramientas —`registrar_pedido`, `buscar_productos`, `consultar_temas`— quedan 18 de 20 clases en verde y las otras seis no mueven una celda; `peso_de_la_cadena.py` dice qué pieza de salida interviene y con quién se pisa. **LA REGLA DEL CORTE, que sale de lo medido esta semana: se cortan JUNTAS, no de a una.** Las piezas están acopladas y cortarlas de a una hace que cada corte perturbe a las otras —pasó dos veces: arreglar la plata rompió el título huérfano, y un candado de descuento nuevo cortó un renglón de pago e inventó un precio—. **Se borran implementaciones repetidas, NO capacidad:** hay cuatro funciones que vigilan el título sin lista, y `buscar_productos`/`consultar_catalogo`/`ficha_producto`/`ver_compatibilidad` leen el MISMO catálogo cambiando la proyección. **La red son cuatro bancos que corren gratis en minutos**: las_40, ablacion, con_modelo_perfecto y produccion. Si un número baja, se revierte entero. OJO: la ablación mide UN TURNO y no distingue una lista de precios de una cuenta armada, así que `armar_presupuesto` no se toca por esa tabla. **EL INSTRUMENTO YA NO ES CIEGO (FICHA 01, 21-ago): el grafo registra en las SEIS etapas, 32 de 32 nodos, y `peso_del_censo.py` dejó de imprimir el aviso de nodos que no ve** —antes veía 17, todos de `salida`, así que cada corte fuera de esa etapa había que medirlo a mano como se midió la reposición el 18-ago—. Lo que apareció al encender la luz y hay que mirar antes de cortar: `cierre` sale **MUERTO**, corre en los 54 turnos y no mueve el texto en ninguno; `decisor`, `herramientas`, `redactor` y `memoria` salen ESTRUCTURAL al 100%, o sea que no son guardias sino piezas del contrato. La medición se cruzó con el instrumento viejo y coincide: `cuenta_repuesta` da 44%, el mismo número que `peso_reposicion.py` había sacado envolviendo la función desde afuera.
- **ABIERTO** · **Posventa NO se borra: se guarda. Martín lo decidió el 14-ago.** La capacidad entera —plazo de devolución, garantía vigente, validación de CUIT— vive en `reserva/`, que es afuera de `app/` y por lo tanto afuera de todos los instrumentos: ya no la cuenta ningún barrido ni pide declararse. `reserva/README.md` dice qué hay y cómo se vuelve a enchufar, y `tests/test_reserva.py` pone cuatro candados, siendo el que importa que `app/` NO puede importar de ahí. **Lo que falta para enchufarla no es el motor, que está entero: es la fecha de compra**, que hoy el sistema no tiene y que no se puede inventar —`las_40` tiene el caso del pedido #4589 justamente para que el bot no confirme un número que no existe.
- **ABIERTO** · Las funciones que sí se alcanzan pero no llama nadie quedaron DECLARADAS con su motivo en `tests/test_nada_suelto.py`, que se pone rojo si aparece una nueva o si una declarada se vuelve fantasma. Bajaron de 38 a 28 el 14-ago. Las que quedan son casi todas el camino sellado de `guia_pedido` y las piezas de compatibilidad: hay que decidir de a una si se enchufan o se borran, sin que la lista crezca mientras tanto.
- **ABIERTO** · **LA REPETICION: CAUSA Y SINTOMA, LOS DOS CERRADOS EL 15-AGO, y la causa era del CODIGO.** `buscar_productos` devolvia las fichas y CADA UNA traia pegada la misma politica de garantia entera: en un resultado de 3.127 caracteres la garantia viajaba 15 veces, asi que el modelo la repetia porque se la dabamos repetida. Ahora el dato igual en todas las fichas viaja UNA vez, en `igual_en_todos`, cortado en `ejecutar`, que es la unica puerta por la que sale un resultado de herramienta. **Lo que identifica y cobra —id, nombre, precio, stock— no se factoriza nunca**, con test. Medido: el resultado de memorias baja de 3.127 a 2.574 y el mensaje real de Martin de 1.546 a 1.276 caracteres. Se cerro tambien la tabla markdown, que WhatsApp no renderiza y `_sin_markdown` no veia. **Lo que sigue ABIERTO de este tema es UNA sola cosa y esta acotada:** el `garantia_detalle` factorizado sale sin la marca, asi que si un dia dos productos de la misma categoria tienen plazos DISTINTOS, el campo no se factoriza y vuelve a viajar por ficha —es correcto, no miente, pero la repeticion vuelve—. No hay hoy ningun caso asi en el catalogo.
- **ABIERTO** · El tope de largo del piso subió de 1.591 a 1.882 caracteres al entrar la charla real del 12-ago, que es la más pesada que se grabó, y creció otra vez cuando el turno 6 pasó a contestar con la cuenta que antes no armaba. No es una regresión: es el número real de hoy. Es el primero que tiene que bajar cuando se ataque la prioridad 2 (concisión).
- **ESPERA A MARTIN** · Tres pares de temas comparten una palabra y ninguno tiene una seña PROPIA que los separe: `factura`/`precios_iva` con "iva", `pedido_de_fotos`/`placa_video` con "video", `cambio_direccion`/`ubicacion` con "direccion". Hoy la guía los describe a los dos, así que el modelo elige con contexto y no a ciegas; que cada uno tenga su palabra propia es una edición de la FUENTE, no de código.
- **ABIERTO** · Los barridos y qué cubre cada uno se leen en `INVENTARIO_BARRIDO.md`, que lo genera un script y tiene candado doble. CUÁNTOS SON no se escribe en ningún texto —ni acá, ni en el hook, que llegó a decir SIETE cuando eran nueve—: sale de la lista del generador. No se dice "el barrido" sin apellido. Lo que sigue abierto está en su sección de límites; hoy son tres: comparar por mayor/menor un campo de texto con magnitud adentro (`ram` "16GB", `hz` "60Hz"), que es una edición de la FUENTE y no de código; la prosa de la compatibilidad; y los encadenados de más de dos turnos con modelo vivo.
- **ESPERA A MARTIN** · **EL SISTEMA SIN LLM: medido por primera vez, y el número está mucho más arriba de lo que cualquiera hubiera dicho.** `banco_pruebas/puerta_determinista.py` mide, sobre los 51 turnos reales grabados con su `registrar_pedido` al lado, cuánto reconstruye el código SOLO leyendo el mensaje crudo. Los números viven en `banco_pruebas/puerta_piso.json` y no pueden bajar. Lo importante para decidir: los rubros y las cantidades se sacan casi enteros con piezas que ya existen y ya están barridas; **lo que NO tiene ninguna pieza es `pide_precio` y `contradicciones`**, y lo que ninguna pieza ve es **la NEGACIÓN** —"el teclado sacalo, dejame solo los mouse" y el código cuenta el teclado igual—, que es justo lo que `CLAUDE.md` pone como objetivo. Aclaración que evita el malentendido: el modelo declara el pedido ACUMULADO, así que el banco separa lo nombrado hoy de lo arrastrado; sin modelo, lo arrastrado lo pone `construir_estado`, que es determinista y ya tiene barrido. **Un modo sin LLM NO se deploya en paralelo al vivo, eso es la regla 2-bis**; como banco de medición y como modo degradado para cuando el modelo se cae —el 429 que hizo que el bot le echara la culpa al catálogo— no es un camino paralelo.
- **ABIERTO** · **Las cosas a medias se cuentan solas y hoy son CERO.** Lo que queda a medias se escribe como un test que afirma lo que queremos y hoy no pasa, `xfail(strict=True)`, así que **la cantidad es el número de `xfailed` al final de la batería**: gratis, offline, sin un token. `tests/test_a_medias.py` le pone techo y solo puede bajar, y exige que cada marca sea estricta para que el día que se arregle el test se ponga rojo y obligue a sacarla. La regla está en `CLAUDE.md` y su tercera línea es la que importa: no se marca lo que se puede cerrar.
- **ABIERTO** · Los dos `G Pro X` de Logitech no resuelven por modelo pelado: sus tres palabras son una letra, `pro` —que está en la lista de genéricas— y otra letra. Con la marca adelante anda. Sacar `pro` de las genéricas toca el match de los 880 y hay que medirlo con el barrido antes.
- **ABIERTO** · El contrato NO_OMITE hoy repone solo el punto de PRECIO, que era el único que quedaba sin contestar en las charlas grabadas. Los otros tipos —un item que no se nombró, un destino que no se dijo— tienen evidencia y no tienen reposición: hay que ver cuál bloque sellado los contesta antes de sumarlos.
- **ABIERTO** · La vara de CONTENIDO. Medida el 12-ago con 1 repetición: `interpretacion.py` da **entiende 94 / contesta 94** —el 91/61 que decía acá era de antes del trabajo de esta semana—. La única falla que se repite es `criterio_de_origen`, 3 de 6 redacciones, y es del lado de la DECLARACIÓN del modelo: el banco lee los args crudos de `registrar_pedido`, así que no ve la restricción que el código completa desde los filtros. Falta medirlo con más repeticiones y sumar casos de turno largo.
- **ABIERTO** · El banco de turno largo corre OFFLINE sobre el estado, que es determinista: cubre carrito, ancla, orden de lo mostrado, tope de memoria e historial, a 1, 5, 10, 20 y 40 turnos. Lo que NO cubre es la redacción del modelo en un turno 10; eso sigue siendo prueba en real o casete grabado.
- **ABIERTO** · **El turno es de DOS llamadas fijas y el piso ya lo defiende: `llamadas_max` bajo de 5 a 2, asi que las rondas no pueden volver sin ponerse rojas.** Las llamadas del corpus cayeron de 177 a 108 y la latencia medida con la clave gratis dio 7,6 s por turno contra los 21-26 de produccion. **Lo unico que quedo pendiente son DOS PUNTOS del piso, de 493 a 491, y NO son una regresion:** los 15 casetes se grabaron con la arquitectura de cuatro rondas y guardan una llamada que el turno ya no hace, asi que el numero castiga al turno nuevo por no consumirla. Probado: regrabada sola con la clave gratis, `81_charla_real_12ago_cierre` pasa de ser el rojo a 100 sobre 100 y el reparto de envios vuelve a resolver. **Los dos puntos se recuperan regrabando las 15, y eso necesita clave** —la paga esta cerrada hasta que se mueva la aguja y la gratis se agoto a mitad de la tanda—. Dos trampas medidas ese dia: el script graba TODOS los guiones si no se le pasan los nombres, creo 65 casetes de mas y hubo que restaurar con git; y la clave paga no se toca.
- **ESPERA A MARTIN** · Los prompts al modelo siguen en el código a propósito (`_INSTRUCCION*`, `_SISTEMA*`). Moverlos a la fuente es decisión suya, no un olvido.
- **ESPERA A MARTIN** · `villa, Buenos Aires` resuelve a un CP que el cliente no nombró. Es previo a los arreglos del 11-ago y de daño chico (misma tarifa de interior), pero es el sistema eligiendo por el cliente. Se arregla en `geo_cp.resolver`.

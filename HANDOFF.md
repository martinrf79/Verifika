# HANDOFF — estado del proyecto para retomar rápido

Leer esto primero. Resume todo lo hecho para no re-derivar contexto.

## >>> PLAN DE CIERRE Y SIMPLIFICACION (sesion 2026-06-03, EMPEZAR POR ACA) <<<

DIAGNOSTICO honesto: las soluciones estan CONSTRUIDAS Y PROBADAS LOCAL pero NO deployadas ni validadas en prod. El proyecto construye y prueba, pero no cierra el loop (este HANDOFF esta lleno de "hecho, probado local, deploy pendiente"). Por eso nunca se siente terminado. El bot NO esta listo para vender: en single-turn aguanta (red-team 0 alucinaciones graves en 77 preguntas), pero en MULTI-TURNO falla (caso Jorge: sobre-prometio dia de entrega e invento retiro en local). Los dos fallos caen en clases que YA tienen verificador (hechos y servicios) pero estan en shadow/off, por eso pasan. Ademas hay laberinto de flags (~40) que esconde fallas. NO ofrecer todavia.

AVANCE 2026-06-03 (rama simplify/remove-dead-flags, 4 commits, bateria 11/11 en cada paso):
- HECHO checkpoint git (commit 3f0675d): todo el trabajo sin commitear quedo a salvo. gitignore arreglado (.secrets*.env y embeddings.json se ignoran).
- HECHO simplificacion parcial: borrados PROMPT_LEAN, SALUDO_DIRECTO, SOLVER_HISTORIAL_LEAN, SOLVER_HIST_MAXCHARS (-164 lineas, ramas muertas + script prueba_saludo_directo.py). PRECALENTAR_CACHE se MANTIENE (optimizacion util, no era perdedor). EMBEDDINGS se MANTIENE (no estoy seguro de que nunca haga falta: sirve para concepto puro a catalogo grande; off, pesa poco). Asi que el unico gordo que queda por BORRAR es INTERPRETE_RICO (entrelazado con interprete_ancla que SE MANTIENE, hacerlo con cuidado).
- HECHO regla siempre-on en _SYSTEM_PROMPT_TEMPLATE (sin flag): no prometer dia de entrega, citar plazo en dias habiles de la FAQ, no inventar retiro en tienda online. Ataca el caso Jorge en el origen. Se aplica al deployar.
- HECHO AUTOFIX del verificador de hechos: en on, si caza una regla mal narrada, repromptea con el dato correcto y solo cae al fallback si el reintento falla. Convierte "no responde" en "responde bien". Falta validar la conducta del reintento con LLM (deploy/shadow).
- PENDIENTE de simplificacion: INTERPRETE_RICO (borrar) y Balde 2 (colapsar ~13 ganadores a fijo, confirmar antes el env de Cloud Run). Trivial: borrar el string _SYSTEM_PROMPT_LEAN y la funcion _comprimir_contenido, ya sin uso.

PLAN, en ORDEN (no saltear):
0. CHECKPOINT git. El arbol esta sucio con trabajo de sesiones previas SIN commitear (agent.py, verificador.py, tools.py, interpretador.py, interprete_ancla.py, orchestrator.py) + lo nuevo (verificador_hechos.py, verificador_servicios.py). Rama creada: simplify/remove-dead-flags. Commitear el estado actual ANTES de borrar nada, para tener punto de retorno.
1. SIMPLIFICAR (amnistia de flags). Barato y seguro: se valida con bateria_robustez (11/11, GRATIS, sin DeepSeek). NO quema plata. ~medio dia a un dia. HACERLO CON HERRAMIENTA BARATA (Gemini/Cursor/Codex), es mecanico, NO gastar Opus en esto.
   - BORRAR (experimentos perdidos, ~8): INTERPRETE_RICO (NO-GO confirmado, el bloque mas gordo, entrelazado en interpretador/interprete_ancla/orchestrator), EMBEDDINGS_ON+PROVIDER+MODEL (medido, no aporta), PROMPT_LEAN (+_SYSTEM_PROMPT_LEAN en agent.py), SALUDO_DIRECTO, SOLVER_HISTORIAL_LEAN+SOLVER_HIST_MAXCHARS, PRECALENTAR_CACHE (en main.py, OJO default true, mirar que hace).
   - COLAPSAR a fijo y sacar flag (ganadores en true permanente, ~13): CALC_PORCENTAJES, ENVIO_GRATIS_AUTO, TOOL_LISTAR_CATALOGO, VERIFIKA_FULL_FAQ_EVIDENCE, PROPOSER_IGNORA_CANTIDAD, VERIFIKA_RECONCILE_NUMBERS, SOLVER_USA_PRESENTACION, FAQ_KEYWORD_FIRST, CIERRE_COMPLETO, CIERRE_PRECIO_PRIMERO, VERIFIKA_EVIDENCE_FROM_TOOLS, CLEAN_MARKDOWN, ASYNC_LLM_OFFLOAD. OJO: fijar el valor que usa PROD (confirmar env de Cloud Run), no el default del codigo (ej CALC_DEFENSIVA esta ON en prod aunque default false).
   - MANTENER (no son grasa): VERIFICADOR_MODE, VERIFICADOR_SERVICIOS, VERIFICADOR_HECHOS, AUTOFIX, PROMPT_VENTA, CALC_DEFENSIVA, INTERPRETE_ANCLA_CATALOGO, USE_VERIFIKA (checker futuro), providers/timeout/HISTORY_LIMIT/umbral/DIAG_TRACE.
   - De ~40 perillas quedarian ~15-18. Batería verde despues de CADA corte.
2. AUTOFIX del verificador de hechos: hoy en on BLOQUEA al fallback; deberia repromptear al solver con el plazo/dato correcto para conservar la venta (igual que el autofix de plata y de servicios).
3. Compuertas de shadow a ON: VERIFICADOR_SERVICIOS y VERIFICADOR_HECHOS. Eso frena los fallos de Jorge.
4. VALIDAR EN MULTI-TURNO: construir el simulador conversacional (otro LLM como cliente, perfiles + objetivos de ruptura, 10-15 turnos, juez que evalua la charla entera contra la ficha de hechos Y contra lo que el bot prometio antes). Cliente simulador con modelo BARATO; el cuello es el JUEZ, no generar. Aca aparecen los bugs multi-turno (contradiccion entre turnos, caverse bajo presion). NO subir la memoria a 16 turnos: agrega latencia y mas superficie de contradiccion, poco valor.
5. RECIEN AHI ofrecer el bot.

ESTADO de las piezas nuevas de esta sesion (todas detras de flag default off, prod intacto):
- verificador_hechos.py NUEVO + flag VERIFICADOR_HECHOS off/shadow/on + test scripts/prueba_hechos.py (9/9). Caza reglas mal narradas (dia de entrega, plazo en horas, detalle de pago). Red-teameado 2 veces: tenia falsos positivos, corregido por CLASE (subjuntivo "llegue" no es promesa; hedges). 0 falsos positivos en 77 respuestas reales. Ver memoria verificador-hechos.md.
- Red-team: data/preguntas_redteam_martin.txt (40) y preguntas_redteam_martin2.txt (37). Reports molino_redteam.json y molino_redteam2.json. Bot solido en single-turn.

NOTA herramienta: Anthropic pego el limite semanal. Para seguir: tarea mecanica (amnistia) -> herramienta barata; razonamiento/validacion -> premium. Alternativas a Opus evaluadas: Gemini/Antigravity (mas barato, nivel Opus), OpenAI Codex (sirve tambien para probar OpenAI en el bot), Cursor Pro.

## Qué es
Bot de ventas WhatsApp/Telegram. FastAPI en Cloud Run.
- Servicio Cloud Run: `agente-bot` | Proyecto GCP: `memory-engine-v1` | Región: `southamerica-east1`
- Base: Firestore. Tienda en uso: `verifika_demo` (50 productos, 22 FAQ).
- LLM: DeepSeek (deepseek-chat). Carpeta local del proyecto: `C:\Users\marti\Downloads\claude code verifika\agente-v4`

## Cómo se deploya (desde la notebook, sin zip)
```
cd "C:\Users\marti\Downloads\claude code verifika\agente-v4"
gcloud run deploy agente-bot --source . --region southamerica-east1 --allow-unauthenticated
```
`.gcloudignore` ya excluye winvenv, .secrets*.env y zips. Cambiar solo una variable de entorno: `gcloud run services update agente-bot --region southamerica-east1 --project memory-engine-v1 --update-env-vars=NOMBRE=valor`

## Arquitectura (flujo de un mensaje)
1. Interpretador (LLM): detecta intención y etapa de venta. Provider configurable.
2. Solver (DeepSeek + tools): busca, calcula y redacta. Usa herramientas.
3. Calculadora `calculate_total`: ÚNICA fuente de números, con PROOF. Maneja porcentajes (descuento), rangos (envío), envío gratis.
4. Verificador determinista (`app/core/verificador.py`): LÍNEA CERO anti-alucinación, por CÓDIGO. Exige que cada cifra de dinero salga del catálogo, FAQ o PROOF. Gatea la respuesta. Modo on/shadow/off.
5. Leads/Cierre (`app/core/leads.py`, `app/core/cierre.py`): captura nombre, teléfono, dirección, pago; avisa al dueño con la orden.
Anti-alucinación vive en el CÓDIGO (verificador), no en el prompt.

## Flags clave (app/config.py) — estado deseado en prod
- VERIFICADOR_MODE=on  (el código gatea; Checker LLM desconectado)
- LLM_PROVIDER=deepseek , INTERPRETER_PROVIDER=deepseek
- PROMPT_LEAN=false  (prompt original probado; lean disponible para bajar latencia)
- CALC_PORCENTAJES, TOOL_LISTAR_CATALOGO, VERIFIKA_FULL_FAQ_EVIDENCE, PROPOSER_IGNORA_CANTIDAD, VERIFICADOR_RECONCILE_NUMBERS, SOLVER_USA_PRESENTACION, FAQ_KEYWORD_FIRST = true
- AUTOFIX=false (opcional), CIERRE_COMPLETO=true
- USE_LEADS, USE_INTERPRETER, USE_VERIFIKA: setearlos en prod por env. Para CIERRE de venta: USE_LEADS=true + OWNER_TELEGRAM_CHAT_ID=<chat dueño>.
- MAX_TOOL_ITERATIONS=6, LLM_TIMEOUT_SECONDS=45
- Gemini wired (GEMINI_API_KEY/MODEL/BASE_URL) pero SIN cuota (requiere pagar). Groq wired pero límite diario gratis + tool calling flojo en el solver.

## Pruebas (corren en la notebook, no en Cloud)
- `winvenv\Scripts\python.exe scripts\bateria_robustez.py`  → núcleo determinista, 11 escenarios, sin credenciales. Correr ANTES de cada deploy. Cubre el camino SIN calculadora defensiva.
- `winvenv\Scripts\python.exe scripts\banco_casos.py`  → banco de casos acumulable: tabla de casos + generador de combinaciones con invariantes (335 combos) + casos reales desde data\casos_reales.json (con total esperado). Métricas a reports\banco_metrics.json e historial a reports\banco_history.jsonl. Corre con CALC_DEFENSIVA activa (config objetivo). Marca PEND lo no implementado y LISTO? si un pendiente pasa solo. Sin credenciales.
- `winvenv\Scripts\python.exe scripts\dashboard.py`  → lee el historial y muestra estado por componente y tendencia. Sin credenciales.
- `winvenv\Scripts\python.exe scripts\clasificar_casos.py --dry-run`  → carril modelo: clasifica conversaciones reales (data\conversaciones_raw.jsonl) con DeepSeek por tipo/dificultad y marca candidatas a regresión. USA DeepSeek (gasta), correr con datos exportados. Los candidatos de cálculo se pasan a mano a data\casos_reales.json.
- `scripts\prueba_interprete.py --tienda verifika_2k [--embeddings] [--preguntas archivo]`  → prueba SOLO el interpretador con DeepSeek contra un set de preguntas (etiquetado .jsonl o crudo .txt). Mide intención, resolución de producto y categoría por sinónimos, y marca productos inventados. NO toca Firestore. Acepta .txt con una pregunta por línea para preguntas reales sin etiqueta.
- `scripts\prueba_bot_completo.py --tienda verifika_2k --preguntas archivo`  → BOT ENTERO end to end (interpretador + solver + tools + calculadora + verificador), devuelve la respuesta tal cual le llega al cliente. Sin Firestore ni Telegram (todo monkeypatched, leads off). Guarda reports\respuestas_bot_completo.json. Es la prueba de integración real.
- `scripts\generar_catalogo_2k.py`  → genera el catálogo simulado de 2000 productos (50 reales de verifika_demo + 1950 procedurales, sin tokens) en data\clientes\verifika_2k\. `scripts\generar_embeddings.py --tienda verifika_2k`  → genera los vectores OpenAI del catálogo (usa .secrets4.env, centavos, una vez) a data\clientes\{tienda}\embeddings.json.
- `scripts\prueba_modelo.py [escenarios]`  → solver real con DeepSeek (.secrets.env), adversarios.
- `scripts\bateria_modelo_100.py 100`  → 100 pruebas variadas (DeepSeek).
- `scripts\prueba_gemini.py` / `prueba_groq.py` / `prueba_interp_groq.py` → pruebas de provider.
- Claves locales: `.secrets.env` (DeepSeek, funciona). `.secrets1.env` (Groq). `.secrets2/.secrets3.env` (Gemini, SIN cuota). `.secrets4.env` (OpenAI, sirve para embeddings). NUNCA subir, NUNCA pegar en el chat.

## Observabilidad
- trace_id en todo, tiempos por etapa (t_interpret_ms, t_solver_ms, etc.), campo `outcome` en message_completed, eventos diag_verificador/diag_verifika (con DIAG_TRACE=true).
- Sentry cableado (SENTRY_DSN). Queries de KPIs en `scripts/kpis.txt`.

## Estado y temas abiertos
- HECHO y probado: anti-alucinación robusta (no se come trampas de precio, slang, typos, inyección), cierre de venta, observabilidad, autofix. Una venta real cerrada OK.
- LATENCIA: RESUELTA (ver sesión 2026-06-01). Causa era CPU throttling de Cloud Run sobre el procesamiento en segundo plano; ahora se procesa dentro del request (PROCESAR_EN_REQUEST=true), gratis. El solver bajó de ~70s a ~3-8s. DeepSeek NUNCA fue el problema.
- ABIERTO: (1) plan de pruebas/certificación: EN MARCHA. Hecho: banco_casos.py (tabla + 335 combinaciones con invariantes + casos reales acumulables con total esperado + métricas por componente), dashboard.py (estado y tendencia), clasificar_casos.py (carril modelo con DeepSeek, listo pero sin correr). Falta: exportar conversaciones reales de Firestore a data\conversaciones_raw.jsonl y enganchar la corrida del Solver real contra los casos reales (hoy prueba_modelo.py y bateria_modelo_100.py ya corren el Solver con DeepSeek). (2) calculadora "defensiva": HECHA con P1 a P5, detrás de flag CALC_DEFENSIVA (default false), en app\core\calc_defensiva.py. Cubre P1 deduplicar extra repetido, P2 rechazar cantidad cero/negativa, P3 normalizar capitalización del concepto, P4 fusionar producto repetido, P5 elegir un solo envío por destino (el destino lo detecta el orchestrator por keywords y lo inyecta por contextvar, el LLM no lo elige; si el destino no es claro y hay dos envíos, rechaza para preguntar). Diseño en CALCULADORA_DEFENSIVA_DISENO.md. Para activar en prod: --update-env-vars=CALC_DEFENSIVA=true (una variable, sin coma), con tu OK. Verificado local: banco 14/14 OK con CALC_DEFENSIVA activa, bateria_robustez 11/11 con el flag apagado. (3) fortificación de código: Bandit, Ruff, Safety (dev tools, no van a requirements). (4) integrar Mercado Pago: link de pago + webhook de confirmación + posventa. (5) evaluar Gemini cuando haya cliente (más rápido, co-ubicado en Google).

## Sesion 2026-06-03: defensas sistemicas (interpretacion rica, confirmacion, servicios), molino, A/B
CONSIGNA de fondo (Martin): dejar de parchar por caso (lista negra, whack-a-mole), pasar a grounding por clase (lista blanca). Las preguntas son infinitas, las CLASES de error son pocas: entendio mal, afirmo algo que no esta en la fuente, o debio preguntar y adivino. Una defensa por clase.

- DEFENSA 1, interpretacion rica. Flag INTERPRETE_RICO (default off). El interprete (DeepSeek, sin GPT) devuelve un SUPERSET del JSON viejo con slots cerrados: intenciones (lista), items [{referencia,categoria,producto_resuelto,cantidad,confianza}], destinos [{texto,cajon razonado,confianza}], atributo_consultado, forma_pago, pide_descuento, ambiguedades. Archivos: app/config.py, app/core/interpretador.py (construir_prompt_interpretador_rico + normalizar_rico), app/core/interprete_ancla.py (anclar_items: ancla cada item al catalogo, nombre exacto resuelve y deja de ser ambiguo). El orchestrator inyecta los slots al Solver (_contexto_slots) para que adivine menos.
- DEFENSA 3, confirmar antes de cotizar. Mismo flag. En orchestrator helpers _slots_ambiguos_decisivos + _intencion_de_compra + _respuesta_confirmacion_rica: si hay un slot decisivo ambiguo Y el cliente quiere cotizar Y no hay producto_resuelto, short-circuit con read-back ("decime cual exactamente queres de...") antes del Solver. Preguntar, no adivinar.
- DEFENSA 2, grounding de servicios. AUTOFIX extendido al verificador de servicios (reintento guiado antes del fallback). Negacion del verificador de servicios ahora por CLAUSULA, no por ventana de chars (la de 22 bloqueaba declines honestos como "No tenemos servicio de instalacion a domicilio"). Falta la version general (Proposer+Checker para todo lo no numerico).
- Bug de CUOTAS arreglado en tools.py calculate_total: un valor de FAQ con unidad NO monetaria (ej cuotas) ya NO se suma al total ni se muestra con $, es informativo. Antes daba "Cuotas sin interes: $6" y Total $90.006. Es correctitud, sin flag, SIEMPRE activo.
- MOLINO de evaluacion (infra reutilizable): scripts/molino.py (runner CONCURRENTE, semaforo, flags por env para A/B, planilla por grupo) + data/molino_set.jsonl (256 preguntas) + data/preguntas_dificiles2.txt (190 plantillas nuevas: devolucion, manual pdf, factura A, mojar, ruido, tornillos, consumo, original-por-comentarios). Concurrencia 10 = 256 en 5 min, misma efectividad. Correr: MOLINO_TAG=x [flags] winvenv\\Scripts\\python.exe scripts\\molino.py.
- A/B BASELINE vs NUEVO (256, v4-flash, verifika_2k): casi identicos. baseline 255 ok / 1 fallback / 0 crash; nuevo 252 ok / 3 confirmaciones / 1 fallback / 0 crash. Con v4-flash las defensas NO dan mejora agregada medible, dan GARANTIA/determinismo. Valor real en la cola larga rara y en modelos mas debiles. El modelo es capaz, las defensas aseguran.
- TESTING: molino y desarrollo sobre verifika_2k (2000). Prod sigue verifika_demo (50, online pura). Antes de prender INTERPRETE_RICO en prod, pasar el molino sobre demo (las defensas se comportan distinto por tienda).

## DEPLOY de esta sesion (2026-06-03)
- Que cambia en prod al deployar: el FIX DE CUOTAS (correctitud, ya no suma las cuotas como pesos) y el fix de negacion del verificador de servicios (que en prod esta en shadow, asi que solo afecta logs). El resto del codigo nuevo queda DORMIDO detras de flags: INTERPRETE_RICO no esta seteado en prod = off; VERIFICADOR_SERVICIOS sigue en shadow; PROMPT_VENTA sigue true; modelo v4-flash. O sea, deploy de bajo riesgo que arregla el bug de cuotas y deja lo nuevo apagado.
- Gate previo OK: bateria_robustez 11/11, prueba_servicios 16/16, imports limpios, flags nuevos default off.
- Comando: gcloud run deploy agente-bot --source . --region southamerica-east1 --allow-unauthenticated
- Para activar defensas mas adelante (con tu OK y tras molino en demo): --update-env-vars=INTERPRETE_RICO=true ; y VERIFICADOR_SERVICIOS de shadow a on. Una variable por comando.

## MIGRACION DEEPSEEK V4 — URGENTE antes del 24-jul-2026 (sesion 2026-06-02)
- deepseek-chat y deepseek-reasoner SE DAN DE BAJA el 24 de julio de 2026. El bot usa deepseek-chat (config DEEPSEEK_MODEL default, endpoint https://api.deepseek.com/v1). Hay que migrar a deepseek-v4-flash (recomendado, rapido y barato) o deepseek-v4-pro (mas potente, mas caro). NOTA: "v4 flash" ES de DeepSeek, antes se confundio con Gemini, error corregido.
- BLOQUEADOR encontrado al probar: los v4 traen RAZONAMIENTO (thinking) por default. Rompe el solver (fuerza tool_choice=required en la 1a vuelta, thinking lo rechaza con error 400) y el interpretador (el razonamiento se come los tokens, JSON vacio). Y suma latencia, lo que peleamos por bajar.
- RESUELTO: se apaga el thinking con extra_body={"thinking":{"type":"disabled"}}. Implementado: helper deepseek_extra_body(model_name) en app/config.py + flag DEEPSEEK_THINKING (default false = non-thinking), aplicado en agent.py _call_llm y interpretador.py _llamar_llm. Solo toca modelos con "v4" en el nombre; deepseek-chat y otros providers quedan IGUAL (extra_body vacio, cero cambio de conducta). En non-thinking, v4-flash se porta como el chat viejo: acepta tool_choice required, llama tools, rapido.
- VALIDADO: smoke de 15 preguntas dificiles con DEEPSEEK_MODEL=deepseek-v4-flash, 15/15 limpio, tool calling OK, numeros correctos, calidad a la par del chat. Bateria 11/11, prueba_servicios 16/16. Reporte en reports/smoke_v4flash.txt.
- A/B COMPLETO 120 preguntas dificiles, chat vs flash, mismas condiciones (PROMPT_VENTA=true, VERIFICADOR_SERVICIOS=on, verifika_2k): EMPATE LIMPIO. Los dos 120/120 sin fallback, cero alucinaciones, cero numeros inventados, cero diferencias en bloqueo. Calidad equivalente; flash levemente mas corto (prom 525 vs 544 chars). VEREDICTO: deepseek-v4-flash non-thinking es reemplazo validado de deepseek-chat. Reportes en reports/ab_chat.json y reports/ab_flash.json.
- EXPERIMENTO THINKING (v4-flash modo razonador, 30 preguntas: 15 dificiles + 15 comunes, data/preguntas_mix30.jsonl): rapido 195s (~6,5s c/u) vs thinking 522s (~17,4s c/u), 2.7x mas lento. Los dos 0 fallbacks en ambos grupos. Calidad: empate en factuales y trampas; en consultas ABIERTAS de asesoramiento (que notebook para arquitectura, algo para editar) el thinking es un poco mas consultivo, mejora marginal. VEREDICTO: para bot de ventas NO compensa, la mejora es marginal y el costo de latencia enorme (justo lo que peleamos por bajar). Default = non-thinking. El thinking queda soportado por llamada (helper acepta think=True, _call_llm pasa a tool_choice auto cuando piensa, interpretador sube max_tokens a 2000) por si algun dia se quiere pensar SOLO en asesoramiento abierto. Reportes reports/mix_fast.json y reports/mix_think.json.
- PARA MIGRAR (listo para ejecutar): cambiar el default en config.py a deepseek-v4-flash y/o env. Prod: gcloud run services update agente-bot --region southamerica-east1 --update-env-vars=DEEPSEEK_MODEL=deepseek-v4-flash (una variable, sin coma). El flag DEEPSEEK_THINKING queda en false (non-thinking). Antes del 24-jul-2026.

## Sesion 2026-06-02 (cont.): servicios por codigo + capa de venta en el prompt
- VERIFICADOR DE SERVICIOS (codigo, segunda linea anti alucinacion): cierra el agujero que el verificador de plata no cubre, las promesas de SERVICIOS que la tienda no ofrece (empaque para regalo, instalacion, entrega en mano, retiro en local de tienda online, garantia extendida, servicio tecnico, prueba previa). Archivo app/core/verificador_servicios.py, codigo puro sin LLM. Catalogo curado de servicios riesgosos con gatillos (frases en la respuesta) y respaldo (substrings que en la evidencia de la tienda prueban que SI lo ofrece). Arma el corpus desde la FAQ y los productos (misma evidencia que el verificador de plata). Negacion en ventana de 22 chars no marca ("no hacemos envoltorio" pasa). Ante la duda NO marca (un falso bloqueo rompe UX). Flag VERIFICADOR_SERVICIOS off/shadow/on, default off, INDEPENDIENTE del VERIFICADOR_MODE de plata. Wired en orchestrator despues del verificador de plata; en on gatea al VERIFIKA_FALLBACK_MESSAGE (sin AUTOFIX, una promesa inventada no se arregla recalculando). Conviene con VERIFIKA_FULL_FAQ_EVIDENCE=true (ya en prod). Test scripts/prueba_servicios.py 12/12 sin Firestore. Bateria 11/11 con el flag off. Rollout: arrancar en shadow (evento de log servicios_shadow), medir, despues on. NO necesita deploy para probarse local.
- CAPA DE VENTA EN EL PROMPT (Regla 9): el prompt era casi todo guardarrail (no inventes, no calcules), sin guia de COMO vender. La persuasion es capa de prompt, no de codigo. Nuevo bloque _REGLA_VENTA en app/core/agent.py, apendice que NO toca las 8 reglas probadas, detras de flag PROMPT_VENTA (default false). Ensena 5 movimientos sin inventar nada: tranquilizar la duda tecnica con lo del catalogo, afirmar compatibilidad y originalidad desde las specs, leer la senal de compra y avanzar al cierre, pivotear a lo que SI hacemos cuando piden un servicio inexistente, y preguntar una cosa corta en vez de suponer el uso. Se mide con A/B en prueba_bot_completo.py antes de prod.
- SET DE PRUEBA NUEVO: data/preguntas_dificiles.txt, ~120 preguntas dificiles reales (slang, typos, miedos tecnicos como "se quema a 220", compatibilidad windows 7, original o replica, "paso a buscar x el local" que es trampa de retiro en tienda online, "me lo mandas armado", "si compro 2 me haces precio", regateo, garantia por mal uso). Correr con scripts/prueba_bot_completo.py --tienda verifika_2k --preguntas data/preguntas_dificiles.txt. Usa DeepSeek (gasta), pedir OK.
- AJUSTES del verificador de servicios tras correr datos reales: (a) agregado servicio "armado" (gatillos te lo mando armado, viene armado, lo armamos; respaldo solo frases de armado de PRODUCTO porque la FAQ dice "armamos el contacto" y "armamos cotizacion", otro sentido). (b) el matcheo de frases ahora es por PALABRA COMPLETA (regex con limites de palabra), no substring, porque "armado" matcheaba dentro de "desarmado" y "armamos" dentro de "desarmamos", justo lo que dicen los clientes ("viene desarmado en la caja"). Test prueba_servicios.py 13/13.
- SMOKE del bot completo (15 preguntas dificiles, verifika_2k, PROMPT_VENTA=true + VERIFICADOR_SERVICIOS=on): 15/15 sin fallback ni bloqueo, todo verificado. La capa de venta tranquiliza los miedos tecnicos (220v, calienta, equipo viejo) con la ficha, afirma compatibilidad y originalidad, y pivotea cuando no hay un servicio. OJO con dos cosas: (1) las respuestas salen LARGAS, la regla de estilo pide 1 a 3 oraciones y la capa de venta las infla, ajuste fino de prompt para WhatsApp. (2) IMPORTANTE: verifika_2k SI tiene punto de retiro en CABA (temas retiro_local y ubicacion en su FAQ), a diferencia de verifika_demo que es online pura. Por eso el bot ofrece "pasar a buscar" y el verificador NO bloquea, es correcto, es tienda-agnostico. No confundir las dos tiendas.
- CORRIDA COMPLETA 120 preguntas dificiles (verifika_2k, PROMPT_VENTA=true + VERIFICADOR_SERVICIOS=on): 120/120 SIN fallback, SIN bloqueo, CERO alucinaciones, cero precios inventados. La capa de venta aguanta el volumen. LECCION de metodo: al revisar parecio que r032 inventaba un servicio de armado de PC a 25000, PERO verifika_2k SI tiene ese servicio en su FAQ (tema armado_pc: "Armamos y testeamos tu PC... por 25000 pesos. Incluye ensamblado, cableado, instalacion de Windows y prueba de stress"). El bot lo cito textual, los dos verificadores acertaron en dejarlo pasar. Antes de gritar alucinacion, leer la FAQ ENTERA de la tienda (2k tiene 28 temas, incluido armado_pc, soporte_tecnico, asesoramiento, mayoristas). Resultados en reports/respuestas_bot_completo.json y reports/run_dificiles_venta.txt.
- FIX defensivo del verificador de servicios tras la corrida: saque la palabra "instalacion" suelta de gatillos y respaldo (aparece inocente en fichas "cables para su instalacion" y en garantia "danos por instalacion"); ahora solo frases de OFRECER el servicio. Agregue gatillos del servicio de armado de PC (armamos la pc, armado y test, instalacion de windows). Test prueba_servicios.py 16/16, incluido el caso r032 y los dos usos inocentes de "instalacion". r032 sigue pasando en 2k porque el servicio es real; bloquearia en una tienda que no lo ofrezca.
- OBSERVACION (no bug, anotar): a 2000 productos casi cualquier monto redondo coincide con algun precio de catalogo, asi que la garantia del verificador de PLATA se afloja un poco a esa escala (un numero inventado podria colar si coincide con un precio). No mordio en estas 120, pero vigilar. La defensa fuerte sigue siendo que el numero salga de la calculadora o la FAQ.
- PENDIENTE de ajuste: las respuestas con PROMPT_VENTA salen LARGAS (prom 528 chars, max 873), chocan con la regla de estilo de 1 a 3 oraciones. Apretar para WhatsApp.
- DEPLOY PENDIENTE de esta sesion: ninguno todavia. Todo detras de flags default off, se prueba LOCAL primero. Cuando los flags demuestren que pescan bien: VERIFICADOR_SERVICIOS=shadow y luego on; PROMPT_VENTA=true tras el A/B. Una variable por comando, sin coma.

## Resuelto: robustez de prompt + verificador, prueba del bot completo (2026-06-02)
- PRUEBA DEL BOT COMPLETO sobre 204 preguntas reales (incluye 23 trampas): de 7 fallbacks a 2, CERO precios/productos inventados. Los 2 que quedan (p063 fravega, p192 te doy 200 mil) son trampas de precio SEGURAS (fallback = no aceptó el precio malo, solo redacción pobre); el modelo se empecina en repetir el número del cliente y el verificador lo bloquea bien. Aspereza menor, revisar con modelo más obediente.
- FIX VERIFICADOR (app\core\verificador.py): el peso en gramos "4.124 g" se confundía con plata (punto de miles + la unidad "g" no estaba en el regex de specs). Agregadas g, gb, tb a _UNIDAD_SPEC_RE. Destrabó respuestas legítimas que mencionan peso (afectaba a cualquier producto con peso). Bateria 11/11 confirmada, no deja pasar plata inventada.
- FIX PROMPT (app\core\agent.py _SYSTEM_PROMPT_TEMPLATE, el activo): Regla 6 ahora prohíbe inventar SERVICIOS/capacidades (empaque para regalo, atención especial, instalación, entrega en mano) y usar un nombre que el cliente no dio. Regla 8 ahora cubre regateo y precios de afuera: NUNCA repetir ni igualar el número que tira el cliente, decir el del catálogo y rechazar con cortesía (repetir el número ajeno hace que el verificador bloquee). Metido dentro de reglas existentes, sin inflar la cuenta de reglas.
- NUEVA CLASE DE ERROR detectada con data real: alucinación de SERVICIOS (no plata, no producto). El verificador NO la cubre (solo cuida dinero). Se ataja por prompt; vigilar.
- AUTOFIX: probado que es la pieza que vuelve confiables las trampas de precio (reintento guiado al bloqueo, baja de 5 a 2 los fallbacks de precio). Recomendado AUTOFIX=true en prod.
- LECCIÓN clave: el prompt baja la frecuencia de error pero NO da garantía dura (la adherencia del modelo no es 100%); la garantía la da el código (verificador + AUTOFIX). DeepSeek NO es el cuello: contexto 131K (usamos ~5%), capaz; los errores eran de prompt. Si algún día se evalúa otro modelo económico, el candidato es Gemini Flash (barato, co-ubicado en GCP) o el salto a DeepSeek V4; A/B con scripts\prueba_bot_completo.py.

## DEPLOY PENDIENTE (de esta tanda)
- Código que va en el deploy: app\core\verificador.py (fix peso) + app\core\agent.py (reglas de prompt).
- Flags a setear en prod tras el deploy (una variable por comando, sin coma): AUTOFIX=true ; INTERPRETE_ANCLA_CATALOGO=true. CALC_DEFENSIVA ya está on. Embeddings quedan OFF (no aportan salvo sinónimos puros; medido).
- Probado sobre verifika_2k (2000 prods). Prod usa verifika_demo (50). Código tienda-agnóstico, pero hacer una pasada corta en prod tras prender los flags.
- Antes del deploy: bateria_robustez 11/11 (confirmado).

## Resuelto en sesión de interpretación + escala (2026-06-01 cont.)
- INTERPRETADOR ANCLADO AL CATÁLOGO: antes el interpretador no veía el catálogo y sus candidatos los inventaba el modelo (proponía productos inexistentes). Ahora, detrás de flag INTERPRETE_ANCLA_CATALOGO (default false), aterriza la interpretación a productos REALES usando la misma búsqueda que el solver (buscar_con_score en app\storage\search.py, comparte _keyword_score). Decide por puntaje del catálogo, sin lista fija de categorías, así escala. Archivo: app\core\interprete_ancla.py. Resuelve uno si domina, deduplica/normaliza, detecta pedido compuesto (2+ categorías nombradas en el mensaje) y lo deja al solver. CLAVE: el anclaje ya NO le impone opciones al solver (no setea ofrecer_opciones); resuelve un producto o se queda callado. Eso borró el bug de la lámpara (frases vagas que enganchaban accesorios irrelevantes y se filtraban a la respuesta) y mejoró el razonamiento del solver. Medido sobre interpretador: ~92% resolución a 50 productos, ~85-89% a 2000.
- BÚSQUEDA SEMÁNTICA (EMBEDDINGS): flags EMBEDDINGS_ON / EMBEDDINGS_PROVIDER (openai) / EMBEDDINGS_MODEL (text-embedding-3-small). buscar_con_score mezcla palabras 60% + significado 40% cuando el flag está on y los productos tienen vector. HALLAZGO medido: en consultas que nombran marca/categoría los embeddings NO aportan (el modelo ya traduce); en lenguaje real de cliente que describe un USO (algo para editar videos) SÍ ayudan, modesto. Default OFF; se prenden si los logs reales muestran muchas consultas de concepto puro. Embeddings de verifika_2k ya generados.
- CATÁLOGO 2000 (cliente simulado verifika_2k): data\clientes\verifika_2k\ con 2000 productos (incluye los 50 de verifika_demo para no romper labels) + FAQ rica de 28 temas (6 cuantitativos). NO pisa verifika_demo, que sigue para las pruebas de cálculo. Formato cliente: productos CSV (único formato del loader crear_cliente.py), FAQ JSON (lleva los valores cuantitativos; el CSV de FAQ los pierde).
- PRUEBA DEL BOT COMPLETO: scripts\prueba_bot_completo.py corre el flujo entero local y devuelve la respuesta al cliente. Sobre 60 preguntas reales de notebooks (data\preguntas_reales_notebook.jsonl): 60/60 sin fallback ni error, CERO productos/precios inventados, contesta bien lo simple y consulta cuando hay duda. Pendiente fino: razonamiento de venta/persuasión (es capa de PROMPT, no de código) y queda 0043-tipo (consultas de edición ofrecen componentes asumiendo PC sin preguntar notebook). Resultados en reports\respuestas_bot_completo.json.
- Flags nuevos en app\config.py: INTERPRETE_ANCLA_CATALOGO, EMBEDDINGS_ON, EMBEDDINGS_PROVIDER, EMBEDDINGS_MODEL. Todos default conservador. Nada deployado de esto salvo CALC_DEFENSIVA que ya está on en prod.

## Resuelto en la última sesión (2026-06-01)
- LATENCIA (causa raíz CONFIRMADA): el bot procesaba en segundo plano (FastAPI BackgroundTask, tras responder 200 al webhook) y Cloud Run estrangula la CPU ahí. Prueba pareada: con throttling el solver tardó 69s; con CPU asignada, 3s. El botón /admin/diag-latencia (llamada pelada a DeepSeek desde Cloud Run) da ~1,2s SIEMPRE, incluso replicando el peso completo del solver → descartado payload/tokens/historial/prompt/red/modelo. FIX gratis: PROCESAR_EN_REQUEST=true (procesa dentro del request, CPU plena, sin pagar always-on). Alternativa paga: --no-cpu-throttling + minScale=1 (~50-65 USD/mes). Detalle en DIAGNOSTICO_LATENCIA.md.
- FALLBACK en cierres de compra grande (>250k): el bot improvisaba el total con envío gratis + descuento y el verificador lo bloqueaba (bien). FIX: calculate_total ahora fuerza envío gratis automático cuando el subtotal de productos supera UMBRAL_ENVIO_GRATIS (flag ENVIO_GRATIS_AUTO=true, umbral 250000), devuelve el total final entero + mensaje_para_llm para que el modelo no recalcule. Regla de negocio: descuento sobre subtotal de productos; envío gratis a cualquier destino si subtotal>250k. Batería F1 reescrita para este caso. 11/11 OK. Verificado en prod: cierre OK.
- Subido MAX_TOOL_ITERATIONS de 6 a 8 (el solver agotaba 6 vueltas en cotizaciones complejas → fallback técnico).
- Nuevos flags (todos en app/config.py): PROCESAR_EN_REQUEST, ENVIO_GRATIS_AUTO, UMBRAL_ENVIO_GRATIS, SALUDO_DIRECTO, PRECALENTAR_CACHE, SOLVER_HISTORIAL_LEAN (estos 2 últimos NO sirvieron para latencia, dejar en false/default). OpenAI cableado como provider opcional (LLM_PROVIDER=openai) pero DeepSeek sigue siendo el modelo.
- Nuevas herramientas de diagnóstico: endpoint /admin/diag-latencia (mide latencia pura desde Cloud Run), scripts/medir_latencia_deepseek.py, scripts/prueba_compresion.py, scripts/prueba_saludo_directo.py, scripts/prueba_interp_saludo_real.py.
- IMPORTANTE rotar: el ADMIN_TOKEN se rotó (estaba expuesto). La OPENAI_API_KEY quedó expuesta en chat viejo, conviene rotarla en platform.openai.com.

## Resuelto en sesión previa (2026-05-30)

## Resuelto en la última sesión (2026-05-30)
- BUG bot no respondía "LLM_PROVIDER inválido: deepseek": era la COMA de PowerShell. Al setear `--update-env-vars=LLM_PROVIDER=deepseek,INTERPRETER_PROVIDER=deepseek` desde PowerShell, la coma mangaba el valor y LLM_PROVIDER quedaba con todo el string pegado. NO era el código ni el prompt. FIX: setear cada variable en un comando aparte, sin coma. REGLA PowerShell: una variable por comando.
- BUG flujo: el bot pedía datos de cierre (nombre/teléfono) ANTES de mostrar el precio cuando el cliente largaba señal de compra al pedir cotización. FIX por código (flag CIERRE_PRECIO_PRIMERO, default true), tres puntos: (a) leads.py caso fuerte se degrada a tibia si no hay presupuesto; (b) orchestrator atajo del interpretador solo cierra si hubo presupuesto previo; (c) orchestrator nunca pisa una cotización fresca con el pedido de datos, muestra precio primero y datos después. Regla: no se piden datos de cierre sin haber mostrado el número.
- Prueba nueva: `scripts\prueba_cierre_orden.py` (simula el escenario sin Firestore, 2/2 OK). Batería determinista sigue 11/11. Ya deployado.
